goal: x ≤ Max x y ∧ y ≤ Max x y

query:
(define-sort Nat () Int)
(declare-const x Nat)
(assert (>= x 0))
(declare-const y Nat)
(assert (>= y 0))
(declare-fun Max (Nat Nat) Nat)
(assert (forall ((_uniq.203 Nat)) (=> (>= _uniq.203 0) (forall ((_uniq.204 Nat)) (=> (>= _uniq.204 0) (>= (Max _uniq.203 _uniq.204) 0))))))
(assert (not (and (<= x (Max x y)) (<= y (Max x y)))))
(check-sat)

result: unknown
goal: x ≤ Max x y ∧ y ≤ Max x y

query:
(define-sort Nat () Int)
(declare-const x Nat)
(assert (>= x 0))
(declare-const y Nat)
(assert (>= y 0))
(define-fun Max ((x Nat) (y Nat)) Nat (ite (<= x y) y x))
(assert (not (and (<= x (Max x y)) (<= y (Max x y)))))
(check-sat)

result: unsat
