goal: x = y → f x = f y

query:
(declare-const y Int)
(declare-const x Int)
(declare-fun f (Int) Int)
(assert (not (=> (= x y) (= (f x) (f y)))))
(check-sat)

result: unsat
Test/Int/Cong.lean:5:2: error: no goals to be solved
