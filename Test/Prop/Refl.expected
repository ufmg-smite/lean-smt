goal: p = p

query:
(declare-const p Bool)
(assert (not (= p p)))
(check-sat)

result: unsat
failed to reconstruct proof: tactic 'apply' failed, failed to unify
  ¬(?p ↔ ?p) → False
with
  ¬¬p = p
p : Prop
⊢ ¬¬p = p
